// 本番（Postgres）用コマンド（CI/EC2上で実行）
// cd backend
// npx prisma migrate deploy --schema=prisma/schema.postgres.prisma
// npx prisma generate --schema=prisma/schema.postgres.prisma



// Prisma schema - ローカル開発は SQLite を想定。production は下のコメントを有効化して DATABASE_URL を本番値にする。


// production 用（PostgreSQL）に切り替える場合は上をコメントアウトして下を有効化する
datasource db {
  provider = "postgresql"
   url      = env("DATABASE_URL")
}

//output = "../generated/prisma" の意味
//Prisma の generator 設定で指定した出力先
//schema.prisma を基準にして相対パスで指定され、Prisma Client の生成物（JS/TS）がそのディレクトリに書き出される
//例えば schema.prisma が backend/prisma/postgres/schema.postgres.prisma にあるなら、生成先は backend/generated/prisma になる
generator client {
  provider = "prisma-client-js"
  //間違い  output   = "../backend/prisma/generated/prisma/postgres"  
  output   = "../../generated/prisma"
}

model StudyRecord {
  id         String   @id @default(uuid())
  title      String
  time       Int
  created_at DateTime @default(now())
}





// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init
